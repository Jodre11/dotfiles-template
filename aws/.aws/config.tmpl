# AWS CLI / SDK config — SSO profiles
# Generated by hydrate.sh from config.tmpl + AWS_PROFILES in config.env.

[sso-session sso]
sso_start_url = __SSO_START_URL__
sso_region = __SSO_REGION__
sso_registration_scopes = sso:account:access
